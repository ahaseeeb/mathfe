export class Level {
  constructor(public id: string,
    public level: number,
    public description: string,
    public age: number,
    public start_maxile_level: number,
    public end_maxile_level: number,    
    public status_id: number, ) {
  }
}