export class User {
  constructor(public id:string, public name:string, public firstname:string, public lastname:string, public contact:string, public email:string,public is_admin:number, public maxile_level:string, public game_level:string, public date_of_birth:string, public last_test_date: string, public next_test_date:string, public image:string) {
  }
}